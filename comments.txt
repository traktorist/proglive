1)
блок switch для контроллера заменил на:
$controller = ucfirst((isset($_GET['c'])) ? $_GET['c'] : 'article');            // немного магии
$controller = new $controller;

По сути я пытаюсь создать создать экземпляр класса, имя которого получается из $_GET['c']
Корректно ли так делать?
Записи "new Article();" и "new Article;" эквавалентны?


2)
Вопрос общего характера:
Если дочерний класс наследует все методы, в том числе и конструктор, от родительского и мы НЕ собираемся переопределять конструктор в дочернем классе, то
целесообразна ли такая запись в дочернем классе? -
function __construct() {
    parent::__construct();
}

Возможно, я не понимаю цель применения строки:
parent::__construct();
